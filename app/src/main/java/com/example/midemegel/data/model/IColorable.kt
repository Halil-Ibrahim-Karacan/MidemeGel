package com.example.midemegel.data.model

/**
 * Renk bilgisi içeren model sınıfları için ortak arayüz.
 */
interface IColorable {
    /**
     * Modelin sahip olduğu renk kodu (String formatında).
     */
    val color: String
}